# Postgres connection for the sc_crimes database.
# Copy to .env (gitignored) and adjust as needed.
DB_USER=db_user
DB_PASSWORD=db_password
DB_HOST=host_goes_here
DB_PORT=1234
DB_NAME=some_db
# TLS mode for the DB connection: require (prod/Neon) or disable (local).
DB_SSLMODE=require
